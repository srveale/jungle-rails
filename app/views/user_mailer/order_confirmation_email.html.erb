<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>This email is to confirm your order at Jungle.</h1>
    <p>
      Order ID: <%= @order.id %>
    </p>
    <p>Your purchase summary:</p>
    <% LineItem.where(order_id: @order.id).each do |line_item| %>
    <p>
    <%= " #{line_item.product.name} x#{line_item.quantity} --> $#{'%.2f' % (line_item.total_price_cents / 100)}" %>
    </p>
    <% end %>
    <p>
      Purchase Total: $<%= '%.2f' % (@order.total_cents / 100) %>
    </p>

    <p>Thanks for your purchase and have a great day!</p>
  </body>
</html>